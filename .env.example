# Server settings
SERVER_PORT=7777

# REQUIRED: Your server's public IP address
# Without this, the server advertises Docker's internal IP and clients cannot connect
MULTIHOME=YOUR_PUBLIC_IP

# Set to "false" to skip update check on container start
UPDATE_ON_START=true

# Session / game settings (passed as CLI args via the ModLoader ServerUtility plugin)
# StartNewGame/LoadSavedGame are handled automatically by the ModLoader
SESSION_NAME=My StarRupture Server
SAVE_GAME_INTERVAL=300

# RCON — leave blank to disable
RCON_PORT=27015
RCON_PASSWORD=changeme
